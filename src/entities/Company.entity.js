export default class {
  constructor({ name, legalForm, employees, address, capital }) {
    this.name = name;
    this.legalForm = legalForm;
    this.employees = employees;
    this.address = address;
    this.capital = capital;
  }
}
